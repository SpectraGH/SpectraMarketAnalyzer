<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Cancelled - Spectra Metal Transactions</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f9fa;
            padding-top: 40px;
            padding-bottom: 40px;
        }
        
        .cancel-container {
            max-width: 700px;
            margin: 0 auto;
        }
        
        .cancel-card {
            background-color: #fff;
            border-radius: 10px;
            padding: 40px;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
            text-align: center;
        }
        
        .cancel-icon {
            font-size: 5rem;
            color: #dc3545;
            margin-bottom: 20px;
        }
        
        .cancel-title {
            font-size: 2.5rem;
            color: #212529;
            margin-bottom: 20px;
        }
        
        .cancel-message {
            font-size: 1.2rem;
            color: #6c757d;
            margin-bottom: 30px;
        }
        
        .btn-primary {
            background-color: #0d6efd;
            border-color: #0d6efd;
            padding: 10px 25px;
            font-weight: 500;
            margin: 10px 5px;
        }
        
        .btn-primary:hover {
            background-color: #0b5ed7;
            border-color: #0a58ca;
        }
        
        .btn-outline-secondary {
            padding: 10px 25px;
            font-weight: 500;
            margin: 10px 5px;
        }
        
        .order-reference {
            background-color: #f8f9fa;
            border-radius: 10px;
            padding: 15px;
            margin: 30px 0;
            text-align: center;
            font-size: 1.2rem;
        }
        
        .order-reference strong {
            color: #0d6efd;
        }
    </style>
</head>
<body>
    <div class="cancel-container">
        <div class="cancel-card">
            <i class="bi bi-x-circle-fill cancel-icon"></i>
            <h1 class="cancel-title">Payment Cancelled</h1>
            <p class="cancel-message">Your payment process was cancelled. No charges have been made to your account.</p>
            
            <% if (orderNumber) { %>
                <div class="order-reference">
                    <p>Your order reference: <strong><%= orderNumber %></strong></p>
                    <p class="small text-muted">You can use this reference if you decide to complete your payment later.</p>
                </div>
            <% } %>
            
            <div class="options mt-4">
                <p>What would you like to do next?</p>
                <div class="d-flex flex-wrap justify-content-center">
                    <a href="/" class="btn btn-primary">Return to Home</a>
                    <% if (orderNumber) { %>
                        <a href="/orders/checkout/<%= orderNumber %>" class="btn btn-outline-secondary">Try Payment Again</a>
                    <% } %>
                </div>
            </div>
            
            <div class="help-text mt-4">
                <p class="text-muted">If you experienced any issues during the payment process or have any questions, please contact our support team at <a href="mailto:support@spectra.com">support@spectra.com</a>.</p>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 