<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Already Processed - Spectra Metal Transactions</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f9fa;
            padding-top: 40px;
            padding-bottom: 40px;
        }
        
        .message-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #fff;
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 0 15px rgba(0,0,0,0.05);
            text-align: center;
        }
        
        .logo {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .logo img {
            max-width: 150px;
        }
        
        .alert-icon {
            font-size: 4rem;
            color: #28a745;
            margin-bottom: 20px;
        }
        
        .title {
            font-size: 1.8rem;
            font-weight: 600;
            color: #333;
            margin-bottom: 15px;
        }
        
        .message {
            font-size: 1.1rem;
            color: #555;
            margin-bottom: 30px;
        }
        
        .order-details {
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
            text-align: left;
        }
        
        .detail-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            padding-bottom: 10px;
            border-bottom: 1px solid #e9ecef;
        }
        
        .detail-row:last-child {
            border-bottom: none;
            margin-bottom: 0;
            padding-bottom: 0;
        }
        
        .btn-receipt {
            background-color: #28a745;
            color: white;
            padding: 10px 20px;
            font-weight: 600;
            text-decoration: none;
            border-radius: 5px;
            display: inline-block;
            margin: 10px;
        }
        
        .btn-receipt:hover {
            background-color: #218838;
            color: white;
        }
        
        .btn-home {
            background-color: #6c757d;
            color: white;
            padding: 10px 20px;
            font-weight: 600;
            text-decoration: none;
            border-radius: 5px;
            display: inline-block;
            margin: 10px;
        }
        
        .btn-home:hover {
            background-color: #5a6268;
            color: white;
        }
    </style>
</head>
<body>
    <div class="logo">
        <img src="https://static.wixstatic.com/media/bb6757_4ca9a0b59e614a1b992bbf048bc8d91d~mv2.jpg" alt="Spectra Metals Logo">
    </div>
    
    <div class="container">
        <div class="message-container">
            <i class="bi bi-check-circle-fill alert-icon"></i>
            <h1 class="title">Payment Already Processed</h1>
            <p class="message">This order has already been paid for. No additional payment is required.</p>
            
            <div class="order-details">
                <div class="detail-row">
                    <strong>Order Number:</strong>
                    <span><%= order.orderNumber %></span>
                </div>
                <div class="detail-row">
                    <strong>Date:</strong>
                    <span><%= new Date(order.createdAt).toLocaleDateString('en-US', {year: 'numeric', month: 'short', day: 'numeric'}) %></span>
                </div>
                <div class="detail-row">
                    <strong>Payment Status:</strong>
                    <span class="badge bg-success">Paid</span>
                </div>
                <div class="detail-row">
                    <strong>Amount:</strong>
                    <span><%= order.calculatedPrice %></span>
                </div>
            </div>
            
            <p>Thank you for your purchase! Your order is now being processed.</p>
            
            <div class="mt-4">
                <% if (order.receiptUrl) { %>
                    <a href="<%= order.receiptUrl %>" target="_blank" class="btn-receipt">
                        <i class="bi bi-file-earmark-pdf"></i> View Receipt
                    </a>
                <% } else if (order.invoiceUrl) { %>
                    <a href="<%= order.invoiceUrl %>" target="_blank" class="btn-receipt">
                        <i class="bi bi-file-earmark-pdf"></i> View Invoice
                    </a>
                <% } %>
                
                <a href="/orders/payment-success/<%= order.orderNumber %>" class="btn-receipt">
                    <i class="bi bi-info-circle"></i> Order Details
                </a>
                
                <a href="/" class="btn-home">
                    <i class="bi bi-house"></i> Return to Home
                </a>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 